// priority: 9001

/**
 * @file Global Functions for use in other scripts.
 */


// Terminus Singularity spell (Black Hole)
global.functions.terminus_singularity = (ctx) => {
  console.log('Singularity spell');
};

// Terminus Space Fold spell (Teleport)
global.functions.terminus_space_fold = (ctx) => {
  console.log('Space fold spell');
};
